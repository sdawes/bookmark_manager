<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
      <% end %>
      <%= yield %> <!-- This then yields to the view which has been called by the controller -->
  </body>
</html>
